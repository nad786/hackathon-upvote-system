import { GetObjectKeyLengthPipe } from './get-object-key-length.pipe';

describe('GetObjectKeyLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new GetObjectKeyLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
